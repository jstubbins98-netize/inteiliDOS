# inteiliDOS
inteiliDOS is a bare-metal x86 operating system written from scratch in C and NASM assembly. It boots directly on real hardware or inside a virtual machine — no host operating system underneath it. It is not a Linux distribution, not a POSIX layer, and not an emulator.

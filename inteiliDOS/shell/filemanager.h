#ifndef FILEMANAGER_H
#define FILEMANAGER_H

/*
 * inteiliDOS -- shell/filemanager.h
 * InteiliFile Manager 1.0 — graphical file system navigator.
 *
 * Controls:
 *   ↑ / ↓       Navigate entries
 *   Enter        Open directory  /  open file action menu
 *   Backspace    Go up one directory level
 *   Escape       Go up one level (quits at root)
 *   Q            Quit
 */
void filemanager_run(void);

#endif /* FILEMANAGER_H */
